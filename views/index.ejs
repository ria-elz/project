<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome - Income Tax Portal</title>
    <link href="/css/style.css" rel="stylesheet">
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/5.1.3/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .landing-header {
            background: url('/images/landing-bg.jpg') no-repeat center center fixed;
            background-size: cover;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.7);
        }
        .header-content {
            text-align: center;
        }
        .header-content h1 {
            font-size: 3rem;
            font-weight: bold;
        }
        .btn-group {
            margin-top: 20px;
        }
    </style>
</head>
<body>

    <div class="landing-header">
        <div class="header-content">
            <h1>Welcome to Educational Platform</h1>
            <p>A platform where you can improve yourself</p>

            <% if (user) { %>
                <div class="btn-group">
                    <a href="/home" class="btn btn-success">Go to Dashboard</a>
                    <a href="/logout" class="btn btn-danger">Logout</a>
                </div>
            <% } else { %>
                <div class="btn-group">
                    <a href="/login" class="btn btn-primary">Login</a>
                    <a href="/register" class="btn btn-secondary">Register</a>
                </div>
            <% } %>
        </div>
    </div>

    <footer class="text-center mt-5">
        <p>&copy; 2025 Income Tax Portal. All rights reserved.</p>
    </footer>

</body>
</html>
